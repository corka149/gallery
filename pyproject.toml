[project]
name = "gallery"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.115.11",
    "jinja2>=3.1.5",
]

[dependency-groups]
dev = [
    "ruff>=0.9.9",
]
